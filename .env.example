# HuggingFace 토큰 (SAM-3 모델 다운로드용)
HUGGINGFACE_API_KEY=hf_xxxx

# CORS 허용 origin (쉼표 구분)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 모델 디렉토리 경로 (루트 기준 상대경로 또는 절대경로)
MODEL_DIR=models

# 가중치 파일 형식: pt | onnx | engine
WEIGHT_TYPE=pt

# config.json 경로
CONFIG_PATH=config.json
